{
  "dependencies": {
    "algorithm-visualizer": "^2.3.9",
    "react-ace": "^12.0.0",
    "sass": "^1.77.8"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "vercel-build": "yarn install && yarn build",
    "start": "concurrently \"npm run start-server\" \"npm run start-visualizer\"",
    "start-server": "cd server && npm start",
    "start-visualizer": "cd visualizer && npm start"
  }
}
